// montando  o guarda-roupa

let guardaroupa = ["camisa preta", "calça", " tenis ","camisa vcermelha","casaco preto"]

// exibindo as opcões do guarda-ropa

console.log("meu guarda-roupa tem: ", guardaroupa)

//selecionado um look para sair

let lookselecionado = [] // look vazio 

lookselecionado.push(guardaroupa[0])

lookselecionado.push(guardaroupa[1])

lookselecionado.push(guardaroupa[2])

lookselecionado.push(guardaroupa[3])

lookselecionado.push(guardaroupa[5])

lookselecionado.push(guardaroupa[4])

// exibindo o look selecionado 

console.log("seu look para sair e:",lookselecionado)